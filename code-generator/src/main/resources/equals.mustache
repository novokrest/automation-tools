    @Override
    public boolean equals(Object obj) {
        if (obj == this) {
            return true;
        }
        if (obj == null || obj.getClass() != getClass()) {
            return false;
        }
        {{className}} other = ({{className}}) obj;
        return {{#fields}}
{{^isFirst}}

            && {{/isFirst}}Objects.equals({{name}}, other.{{name}}){{/fields}};
    }

    @Override
    public int hashCode() {
        return Objects.hash({{#fields}}{{name}}{{^isLast}}, {{/isLast}}{{/fields}});
    }
